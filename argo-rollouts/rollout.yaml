apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: healing-app
  namespace: self-healing
  labels:
    app: healing-app
  annotations:
    notifications.argoproj.io/subscribe.on-rollout-completed.slack: deployments
    notifications.argoproj.io/subscribe.on-rollout-degraded.slack: alerts
    notifications.argoproj.io/subscribe.on-analysis-error.slack: alerts
spec:
  replicas: 3
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: healing-app

  template:
    metadata:
      labels:
        app: healing-app
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: healing-app
          image: ghcr.io/code-and-secure/self-healing-deployment-engine/healing-app:44987fc6018fdd988e8959494f7cda6baed35e95
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080

          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3

          startupProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 12

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name

          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]

          volumeMounts:
            - name: tmp
              mountPath: /tmp

      volumes:
        - name: tmp
          emptyDir: {}

  # ── Canary Strategy with Auto-Rollback ─────────────────────────────────
  strategy:
    canary:
      # Services: stable traffic stays on canaryService=0%, stable=100% until promoted
      canaryService: healing-app-canary
      stableService: healing-app-stable

      # Traffic shaping via Nginx ingress
      trafficRouting:
        nginx:
          stableIngress: healing-app-stable

      # Auto-rollback: revert immediately if any analysis fails
      autoPromotionEnabled: false    # Require all analysis gates to pass

      steps:
        # Step 1: Send 20% traffic to canary, run smoke test
        - setWeight: 20
        - analysis:
            templates:
              - templateName: smoke-test
        - pause:
            duration: 1m

        # Step 2: 50% traffic
        - setWeight: 50
        - pause:
            duration: 2m

        # Step 3: 100% — full promotion
        - setWeight: 100
---
# Stable service receives all traffic initially
apiVersion: v1
kind: Service
metadata:
  name: healing-app-stable
  namespace: self-healing
  labels:
    app: healing-app
    track: stable
spec:
  selector:
    app: healing-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
# Canary service receives only canary pod traffic
apiVersion: v1
kind: Service
metadata:
  name: healing-app-canary
  namespace: self-healing
  labels:
    app: healing-app
    track: canary
spec:
  selector:
    app: healing-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healing-app-stable
  namespace: self-healing
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "10"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "30"
spec:
  rules:
    - host: healing-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healing-app-stable
                port:
                  number: 80
